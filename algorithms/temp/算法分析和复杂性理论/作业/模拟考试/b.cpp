#include <iostream>
#include <stdio.h>
using namespace std;

int M[110][110]={0}, opt[110][110];
int main()
{
    int h, w;
    scanf("%d %d",&h, &w);
    for(int i=1; i<=h; i++)
    {
        for(int j=1; j<=w; j++)
        {
            scanf("%d", &M[i][j]);
            opt[i][j]=1;
        }
    }
    for()
}
